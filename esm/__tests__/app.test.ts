/**
 * We are not doing real unit test here. This is just to proof that our package
 * will be excluded from consuming application unittest
 */
describe('Dummy UnitTests', () => {
  test('Test 1', () => {
    expect(true).toEqual(true);
  });
});